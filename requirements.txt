# ============================================
# Chatbot-CT-Group — Project-specific deps ONLY
# Thư viện dùng chung đã nằm trong ctgroup/python-base:1.0
# Chỉ thêm vào đây nếu project này cần lib RIÊNG
# ============================================

# Hiện tại tất cả deps đã có trong base image
# Thêm project-specific deps bên dưới nếu cần:

langchain-groq
ragas